# Environment Configuration for Spotify MCP Multi-User Server

# Basic Configuration
ENVIRONMENT=dev
TRANSPORT=http
DEPLOYMENT=local

# Server Configuration
HOST=0.0.0.0
PORT=8001
MCP_PATH=/mcp
BASE_URL=http://localhost:8001

# Database Configuration
# For local development (SQLite)
# DATABASE_URL=sqlite:///./spotify_users.db

# For production (PostgreSQL on Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# Security
# Generate a secure key for production: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TOKEN_ENCRYPTION_KEY=generate-a-secure-key-for-production
SESSION_SECRET_KEY=your-session-secret-key-here

# Spotify App Configuration
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Local development
SPOTIFY_REDIRECT_URI=http://localhost:8001/auth/callback

# Production (adjust domain)
# SPOTIFY_REDIRECT_URI=https://hosted-spotify-mcp-server.onrender.com/auth/callback

# Optional: API Configuration
API_KEY_LENGTH=32
TOKEN_EXPIRY_BUFFER=300
